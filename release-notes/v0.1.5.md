# xray-pilot v0.1.5

订阅入口体验与多场景适配。

## 新增

- **可视化订阅页**：浏览器访问 `/sub/{token}` 返回 HTML 信息页，支持亮色 / 暗色主题
- **同链接多格式**：同一订阅 URL 根据访问方式返回 HTML、Base64 或 Clash 配置
- **移动端适配**：订阅页完成手机端布局，支持二维码查看 / 复制 / 保存
- **后台快捷入口**：用户管理页新增「打开订阅页」，便于直接检查用户订阅状态
