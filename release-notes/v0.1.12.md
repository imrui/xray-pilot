# xray-pilot v0.1.12

用户与分组的多对多升级，订阅页交互细节优化。

## 新增

- **用户 ↔ 分组多对多**：单个用户可同时加入多个分组，订阅与节点配置自动汇总并去重
- **用户编辑页**：分组选择改为多选下拉，支持连续勾选不自动关闭、一键清空
- **过期时间编辑**：改为「临时选择 + 确定提交」，支持整块点击打开、重置、今天、清空，过期时间红色提示

## 修复

- 协议密钥配置抽屉「一键生成后未保存草稿丢失」的问题

## 升级

- 启动时自动迁移旧 `users.group_id` 数据到新的 `user_groups` 关系表，无需手动处理
